import * as Defaults from "./core";

export const { initialize } = Defaults;
export const { login } = Defaults;
export const { getOptions } = Defaults;
export const { getProfile } = Defaults;

export default {
  ...Defaults,
};
